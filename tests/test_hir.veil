fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

struct Point {
    x: f64,
    y: f64,
}

impl Point {
    fn new(x: f64, y: f64) -> Point {
        return Point { x: x, y: y };
    }
}

fn main() {
    let result = add(5, 3);
    let point = Point::new(1.0, 2.0);
    return result;
}
